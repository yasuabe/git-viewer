export default function App() {
  return (
    <main className="placeholder-shell">
      <section className="placeholder-panel">
        <p className="eyebrow">Application</p>
        <h1>Git Viewer App Shell</h1>
        <p className="lede">
          本体開発用の入口。DAG view と Diff view の両方をここへ組み込む。
        </p>
      </section>
    </main>
  );
}
